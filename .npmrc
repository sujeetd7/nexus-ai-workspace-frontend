engine-strict=true

# GitHub Packages — Wave 1 platform consumer (Batch 5.1G)
# Non-secret registry mapping only. Do not commit tokens.
# Auth: user-level ~/.npmrc or CI NODE_AUTH_TOKEN.
@sujeetd7:registry=https://npm.pkg.github.com
always-auth=true
