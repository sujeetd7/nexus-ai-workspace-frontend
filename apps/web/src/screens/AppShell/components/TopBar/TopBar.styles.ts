import type { CSSProperties } from "react";

export const topBarStyle: CSSProperties = {
  minHeight: 56,
  display: "flex",
  alignItems: "center",
};

export const topBarInnerStyle: CSSProperties = {
  width: "100%",
};
