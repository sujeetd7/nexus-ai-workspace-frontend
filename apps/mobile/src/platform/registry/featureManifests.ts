import type { FeatureManifest } from "@nexus/shared-types";

/**
 * Statically declared mobile feature manifests.
 * Product features are intentionally absent in Batch 3.4.
 */
export const MOBILE_FEATURE_MANIFESTS: readonly FeatureManifest[] = [];
