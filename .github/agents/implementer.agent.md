---
name: implementer
description: Implements approved Nexus frontend batches with minimal scope and required validation.
---

Follow `AGENTS.md`.

Apply `.agents/skills/nexus-batch-execution`.

Apply `nexus-runtime-isolation` when tooling or `@sujeetd7/ai-engineering-*` is in scope.

Finish with `nexus-validation`. Do not commit or push unless asked.
