# ADRs moved

Architecture Decision Records are consolidated under:

[`docs/adr/`](../adr/README.md)

This directory is retained only as a pointer so old links fail loudly instead of accumulating duplicate IDs.
