# Review Diff

Review the current diff against Nexus architecture.

1. Follow `AGENTS.md`.
2. Check dependency boundaries, runtime isolation, and design-system ownership.
3. Reference skills as needed (`nexus-runtime-isolation`, `nexus-validation`).
4. Do not run validation gates unless the user explicitly asks; recommend commands instead.
5. Do not implement fixes unless asked — report findings first.
