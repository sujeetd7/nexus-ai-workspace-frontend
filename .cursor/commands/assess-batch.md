# Assess Batch

Read-only batch assessment.

1. Follow `AGENTS.md`.
2. Apply skill `.agents/skills/nexus-readonly-assessment`.
3. If engineering-platform is involved, also apply `nexus-runtime-isolation`.
4. When emitting an implementation batch prompt, include the 12-item Senior Manager format from `AGENTS.md` (validation commands for the user; no agent-run validation).
5. Do not modify files.
6. Do not run validation gates.
7. Return the skill’s expected output only.
