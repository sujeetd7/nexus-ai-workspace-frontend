# Figma Implement

Implement UI from an approved Figma frame.

1. Follow `AGENTS.md`.
2. Apply skill `.agents/skills/nexus-design-to-code`.
3. Do not configure Figma OAuth or store MCP credentials in the repo.
4. Finish with skill `nexus-validation` (report recommended user commands; do not execute gates unless asked).
5. Do not commit or push unless asked.
