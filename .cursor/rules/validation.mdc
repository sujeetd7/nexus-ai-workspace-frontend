---
description: Cursor validation gate reminder (user-owned)
alwaysApply: false
---

Validation is user-owned. Apply skill `nexus-validation` to report recommended commands.
Do not run `pnpm verify`, lint, typecheck, tests, or builds unless the user explicitly asks.
Do not claim gates passed when they were not run.
Do not commit or push unless the user explicitly asks.
